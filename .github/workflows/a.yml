name: Host - Recaptcha Rooms

on:
  push:
    branches: [main]
  workflow_dispatch:

concurrency:
  group: haxball-recaptcha-hosts
  cancel-in-progress: false

jobs:
  run-script:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      max-parallel: 11
      matrix:
        include:
          - recaptcha_token: "thr1.AAAAAGlSjvRqx7EFur_cgA.poYnnDOY54A"
            index: 0
          - recaptcha_token: "thr1.AAAAAGlSjv2GEeyPKxt-MQ.zeUV0Fa-EUw"
            index: 1
          - recaptcha_token: "thr1.AAAAAGlSjwMLFe1j3jxLjg.VkGLTkN-PSo"
            index: 2
          - recaptcha_token: "thr1.AAAAAGlSjwsUfrBVZfD1Hg.GsnxW6uN4RE"
            index: 3
          - recaptcha_token: "thr1.AAAAAGlSjxE_5U84bvcgdw.47qbz1Hrdqg"
            index: 4
          - recaptcha_token: "thr1.AAAAAGlSjxgM-dBdaV-Puw.HMcb56QlsZ8"
            index: 5
          - recaptcha_token: "thr1.AAAAAGlSjx9f_zCmiEHrHw.7fVOqNrsrRA"
            index: 6
          - recaptcha_token: "thr1.AAAAAGlSjycr3F99KfVD6Q.R2yKio0YNyY"
            index: 7
          - recaptcha_token: "thr1.AAAAAGlSjz3oeFOwYXLBwA.Wi8_Q62sd9M"
            index: 8
          - recaptcha_token: "thr1.AAAAAGlSj0UKaQRXkJLVbA.gfiYcAqib5E"
            index: 9
          - recaptcha_token: "thr1.AAAAAGlSj1NkGqeMN96lkg.sf2cqQXFy7Y"
            index: 10

    steps:
      - name: 🧩 Checkout repo
        uses: actions/checkout@v4

      - name: 🧹 Limpiar entorno previo
        run: rm -rf node_modules package-lock.json

      - name: ⚙️ Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: 📦 Instalar dependencias
        run: npm install

      - name: 🚀 Ejecutar host
        run: node hosts.js
        env:
          INDEX: ${{ matrix.index }}
          RECAPTCHA_TOKEN: ${{ matrix.recaptcha_token }}
          ROOM_PASSWORD: ""
          PLAYER_NAME: "Teleese"
          PLAYER_AVATAR: "👽"
