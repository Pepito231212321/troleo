name: Host - Recaptcha Rooms

on:
  push:
    branches: [main]
  workflow_dispatch:

concurrency:
  group: haxball-recaptcha-hosts
  cancel-in-progress: false

jobs:
  run-script:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      max-parallel: 11
      matrix:
        include:
          - recaptcha_token: "thr1.AAAAAGk2WOozdn1puTuvpQ.xEY_UZKwhI0"
            index: 0
          - recaptcha_token: "thr1.AAAAAGk2WRYFP7tWl_XCkA.1OY8dDfrN10"
            index: 1
          - recaptcha_token: "thr1.AAAAAGk2WR6jjHHEdQ-sJg.uh7WY8R7gCQ"
            index: 2
          - recaptcha_token: "thr1.AAAAAGk2WSv0ZJjdFqO5Tg.b1eCgEDr3dQ"
            index: 3
          - recaptcha_token: "thr1.AAAAAGk2WT665JsbJ0o49w.lONP8IQqEZ0"
            index: 4
          - recaptcha_token: "thr1.AAAAAGk2WWMiHKmMdEHXAg.iOSfu-s1dSY"
            index: 5
          - recaptcha_token: "thr1.AAAAAGk2WZKU_vsXGOwwiQ.sgMihptXIUc"
            index: 6
          - recaptcha_token: "thr1.AAAAAGk2WZrGIr549rE9QQ.XenLe45Wc3s"
            index: 7
          - recaptcha_token: "thr1.AAAAAGk2WmJDS20gHP-hng.5KBd3-gAau4"
            index: 8
          - recaptcha_token: "thr1.AAAAAGk2WmoqgbfYJN514Q.sIm_fVOfJDo"
            index: 9
          - recaptcha_token: "thr1.AAAAAGk2Wnt_6Fo15iuiQA.nNtjNTagfC4"
            index: 10

    steps:
      - name: 🧩 Checkout repo
        uses: actions/checkout@v4

      - name: 🧹 Limpiar entorno previo
        run: rm -rf node_modules package-lock.json

      - name: ⚙️ Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: 📦 Instalar dependencias
        run: npm install

      - name: 🚀 Ejecutar host
        run: node hosts.js
        env:
          INDEX: ${{ matrix.index }}
          RECAPTCHA_TOKEN: ${{ matrix.recaptcha_token }}
          ROOM_PASSWORD: ""
          PLAYER_NAME: "Teleese"
          PLAYER_AVATAR: "👽"

